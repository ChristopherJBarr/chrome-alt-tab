# App icon

Original AI-generated artwork created for chrome-alt-tab, distributed under this project's MIT license. `app-icon.png` is the transparent source master.

Run `scripts/export-icons.ps1` on Windows to regenerate the Chrome PNGs and multi-resolution Windows ICO using built-in System.Drawing. The generated assets are checked in; building the application needs no image tools.
